version: '3.8'

services:
  # Patchwork Dashboard - Basic Version
  app:
    image: newsmast/patchwork_dashboard_basic:latest
    container_name: patchwork-basic-dashboard
    restart: unless-stopped
    env_file: .env
    environment:
      RAILS_ENV: production
      
    ports:
      - "${EXTERNAL_PORT}:3001"
    volumes:
      - patchwork_basic_storage:/usr/app/storage
      - patchwork_basic_public:/usr/app/public/system
      - patchwork_basic_logs:/usr/app/log
    networks:
      - patchwork_basic_network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:${EXTERNAL_PORT}/health_check || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

volumes:
  patchwork_basic_storage:
    driver: local
  patchwork_basic_public:
    driver: local
  patchwork_basic_logs:
    driver: local

networks:
  patchwork_basic_network:
    driver: bridge